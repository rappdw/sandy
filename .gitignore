# macOS
.DS_Store

# Editors
*.swp
*.swo
*~
.vscode/
.idea/

# Sandy runtime (generated in ~/.sandy/, not in repo)
*.bak

.secrets

# Claude Code's per-workspace settings. Sandy WRITES this file on every launch
# (the crossSessionInbound pin, see CLAUDE.md), so it reappears whether or not
# anyone commits it -- and its contents are a per-machine launch artifact, not
# project configuration.
.claude/settings.local.json

# Working docs (not for the repo)
POLISH_AND_PUBLICITY.md

# Audit output (from test/audit/audit-stress-test.sh)
sandy-audit-*.log

__pycache__
.cachet/

# Local session artifacts (not for the repo)
.mcp.json
connector-reference
.sandy/
